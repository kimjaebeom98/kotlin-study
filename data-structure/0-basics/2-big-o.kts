// 0.2 Big-O
// 실행: kotlin data-structure/0-basics/2-big-o.kts

// TODO: O(1), O(log n), O(n), O(n log n), O(n²) — 각각을 실제로 재현하는 작은 함수 작성
// TODO: 정렬된 배열에서 이진 탐색이 O(log n)인 이유를 직접 짜서 확인
// TODO: 시간 복잡도 vs 공간 복잡도 — 트레이드오프를 보여주는 예제 (예: 중복 체크를 Set으로 vs 배열로)
// TODO: 최선/평균/최악 케이스 — 정렬 안 된 배열에서 값 찾기
// TODO: 배열 임의 접근 O(1) vs 연결 리스트 임의 접근 O(n) 비교 (측정은 1.3에서 연결 리스트 구현 후)
